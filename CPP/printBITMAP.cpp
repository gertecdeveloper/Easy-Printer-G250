#include <iostream>
#include <fstream>
#include <string>

#include <Q

using namespace std;

string printBITMAP(string filePath){

    ifstream myfile;

    myfile.open(filePath);

    if (myfile)
    {
        return NULL;

        string r_bitmap = "";
        //BitArray dots; Pesquisar
        int xHeight;
        int xWidht;
    }

    using (var bitmap = (Bitmap)Bitmap.FromFile(filePath)){

    }

}
